// src/pages/Settings.js
import React from 'react';

const Settings = () => {
  return (
    <div>
      <h1>Settings</h1>
      {/* Add settings options like profile update, theme, etc. */}
    </div>
  );
};

export default Settings;
